{
  "name": "formable",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "./apps/*",
    "./packages/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel --print-label dev:*",
    "dev:docs": "yarn workspace docs run dev",
    "dev:storybook": "yarn workspace docs run storybook",
    "test": "yarn workspace ui run test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
